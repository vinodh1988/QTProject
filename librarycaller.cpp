#include "operations.h"
#include<QDebug>
#include <QCoreApplication>
#include <QDateTime>
/*
int main(int argc, char *argv[])
{
    QCoreApplication app(argc, argv);
    qInfo() << add(34,56) << "is the addition fo add(34,56)";
    qInfo() << sub(70,40) << "is the substraction for sub(70,40)";

    tentimeCaller([]() {
            qInfo() << "I am called back @ " << QDateTime::currentDateTime();
    });
    return 0;
}
*/
